# Setup

1. Criar a base PostgreSQL com `database/schema.sql`.
2. Carregar dados iniciais com `database/data.sql`.
3. Configurar datasource JPA em `persistence.xml`.
4. Compilar e executar:
   ```bash
   mvn test
   ```
